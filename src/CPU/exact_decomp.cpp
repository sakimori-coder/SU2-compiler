#include <bits/stdc++.h>
#include "Rings.cpp"


namespace exact
{
    using matrix2cd = std::vector<std::vector<std::complex<double>>>;
    double sqrt2 = std::sqrt(2.0);
    double isqrt2 = 1. / sqrt2;
}

template<typename T>
std::string exact_decomposition(ZOmega<T> u, ZOmega<T> t, int k){
    std::string S_out = "";
}